## Packages
framer-motion | Essential for the premium iOS 26 style page transitions and smooth micro-interactions.
clsx | Utility for constructing className strings conditionally.
tailwind-merge | Utility to merge tailwind classes without style conflicts.

## Notes
- Using framer-motion heavily for the requested premium, smooth glassmorphic aesthetic.
- The stats increment POST endpoint is called once on the Home page mount to track live views.
- Previews utilize a CSS Grid layout that adapts based on the `size` property of the data.
- Hardcoded FAQs are provided as requested, but seamlessly integrate with any fetched FAQs from the API.
